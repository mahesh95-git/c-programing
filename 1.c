#include<stdio.h>
#include<conio.h>
void sum(int a,int b){
int multi=0;
    printf("multiplication of tow number is:%d",multi=a*b);
}
void main(){
    int a,b;
printf("function with arguments and without return values");
printf("\nprogram to multiplication two number");
printf("\nenter tow number:");
scanf("%d%d",&a,&b);
multi(a,b);
    
}
