#include<stdio.h>
#include<conio.h>
void sum(int a,int b){
int sum=0;
    printf("addition of tow number is:%d",sum=a+b);
}
void main(){
    int a,b;
printf("function with arguments and without return values");
printf("\nprogram to addition two number");
printf("\nenter tow number:");
scanf("%d%d",&a,&b);
sum(a,b);
    
}
